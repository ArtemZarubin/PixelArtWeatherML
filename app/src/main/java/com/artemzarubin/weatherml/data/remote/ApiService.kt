package com.artemzarubin.weatherml.data.remote

interface ApiService {
    // TODO: Define API call methods here later (e.g., for current weather, forecast)
    // Example:
    // @GET("weather")
    // suspend fun getCurrentWeather(
    //     @Query("lat") latitude: Double,
    //     @Query("lon") longitude: Double,
    //     @Query("appid") apiKey: String,
    //     @Query("units") units: String = "metric" // Or "imperial"
    // ): CurrentWeatherResponse // CurrentWeatherResponse will be a DTO data class
}